---

  - name: include ansible-role-nvm role variables
    import_role:
      name: ansible-role-nvm
    vars:
      nvm_profile: "/home/ansible-test/.bashrc"
    become: true
    become_user: ansible-test
